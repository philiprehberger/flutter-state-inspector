name: philiprehberger_state_inspector
description: >-
  Debug overlay showing state changes, rebuild counts, and transition history
version: 0.4.0
homepage: https://philiprehberger.com/open-source-packages/dart/philiprehberger_state_inspector
repository: https://github.com/philiprehberger/flutter-state-inspector
issue_tracker: https://github.com/philiprehberger/flutter-state-inspector/issues

topics:
  - debugging
  - state-management
  - developer-tools

environment:
  sdk: ^3.6.0
  flutter: ">=3.29.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ">=5.0.0 <7.0.0"
